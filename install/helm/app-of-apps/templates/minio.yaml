{{- if .Values.minio.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.minio.applicationName }}
  namespace: {{ .Values.minio.applicationNamespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "{{ .Values.minio.argocd.syncwave }}"
  finalizers:
    - resources-finalizer.argocd.argoproj.io/foreground
spec:
  destination:
    name: ""
    namespace: {{ .Values.minio.destinationNamespace }}
    server: 'https://kubernetes.default.svc'
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m0s
      limit: 10
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  source:
    repoURL: {{ .Values.minio.source.repoUrl }}
    targetRevision: {{ .Values.minio.source.targetRevision }}
    path: {{ .Values.minio.source.path }}
    helm:
      valuesObject:
        {{ toYaml .Values.minio.source.helm.values | nindent 8 }}
{{- end }}
